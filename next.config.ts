import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.pinimg.com", "media.discordapp.net"], // Add the required domain here
  },
  /* config options here */
};

export default nextConfig;
